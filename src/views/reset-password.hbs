<html>
  <head>
    <title>Redefinição de Senha</title>
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }
      .container {
        background-color: #fff;
        padding: 5%;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        width: 30%;
        text-align: center;
      }
      h1 {
        font-size: 28px; /* Aumentado de 24px para 28px */
        margin-bottom: 5%;
        font-weight: bolder; /* Adicionado bolder */
      }
      label {
        display: block;
        margin-bottom: 2%;
        font-weight: bolder; /* Alterado para bolder */
        font-size: 18px; /* Aumentado de tamanho */
      }
      input[type='password'] {
        width: 90%;
        padding: 2%;
        margin-bottom: 5%;
        border: 1px solid #ccc;
        border-radius: 4px;
        font-size: 16px; /* Aumentado de tamanho */
      }
      button {
        background-color: #007bff;
        color: #fff;
        padding: 2% 5%;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 18px; /* Aumentado de tamanho */
        font-weight: bolder; /* Adicionado bolder */
      }
      button:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Redefinição de Senha</h1>
      <form action='/reset-password?token={{token}}' method='POST'>
        <input type='hidden' name='token' value='{{token}}' />
        <label for='newPassword'>Nova Senha:</label>
        <input type='password' id='newPassword' name='newPassword' required />
        <button type='submit'>Redefinir Senha</button>
      </form>
    </div>
  </body>
</html>